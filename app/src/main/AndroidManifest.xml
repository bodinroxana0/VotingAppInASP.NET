<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.datc_p1">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_start2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_start_round"
        android:theme="@style/AppTheme">
        <activity android:name=".Adapters.CandidatesAdapter" />
        <activity android:name=".Activities.StatisticsActivity" />
        <activity android:name=".Activities.FinishActivity" />
        <activity android:name=".Activities.LoginActivity" />
        <activity android:name=".Activities.DetailActivity" />
        <activity android:name=".Activities.VoteActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".Activities.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>